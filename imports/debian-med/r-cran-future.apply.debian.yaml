package: r-cran-future.apply
distribution: debian
release: sid
component: main
version: 1.11.3
source: r-cran-future.apply
homepage: https://cran.r-project.org/package=future.apply
license: unknown
blend: debian-med
description_md5: bccf1fa10be048414b8db6c658ec6761
popcon:
- insts: 1608
  nofiles: 2
  olde: 1198
  recent: 213
  vote: 195
descr:
- package: r-cran-future.apply
  description: apply function to elements in parallel using futures
  long_description: " Implementations of apply(), by(), eapply(), lapply(), Map(),
    mapply(),\n replicate(), sapply(), tapply(), and vapply() that can be resolved
    using\n any future-supported backend, e.g. parallel on the local machine or\n\
    \ distributed on a compute cluster. These future_*apply() functions come\n with
    the same pros and cons as the corresponding base-R *apply()\n functions but with
    the additional feature of being able to be processed\n via the future framework.\n"
  release: sid
  description_md5: bccf1fa10be048414b8db6c658ec6761
  license: unknown
  blend: debian-med
- package: r-cran-future.apply
  description: Anvend funktioner på elementer parallelt via futures
  long_description: " Implementeringer af pply(), by(), eapply(), lapply(), Map(),
    mapply(), replicate(), sapply(), tapply() og vapply() der kan slå op via enhver
    future-understøttet motor, f.eks. parallelt på den lokale maskine eller distribueret
    på en beregningsklynge. Disse future_*apply()-funktioner kommer med de samme fordele
    og ulemper som de tilsvarende base-R *apply()-funktioner, men med den yderligere
    funktion at kunne behandles via future-rammen.\n"
  release: sid
  description_md5: bccf1fa10be048414b8db6c658ec6761
  license: unknown
  blend: debian-med
