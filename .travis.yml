---
sudo: required
language: python

install:
  - sudo apt-get update
  - sudo update-ca-certificates
  #- sudo pip install requests

script:
  # Check website
  - git diff --name-only $TRAVIS_COMMIT_RANGE | python validate.py
  #- python validate.py ./data
